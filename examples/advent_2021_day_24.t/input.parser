unary_intruction = label("type", "inp") + skip(space) + label("var", alpha)

binary_instruction =
  label("type", "add" | "mod" | "div") + skip(space) +
  label("a", alpha) + skip(space) +
  label("b", alpha | int)

array_sep(unary_intruction | binary_instruction, nl)
