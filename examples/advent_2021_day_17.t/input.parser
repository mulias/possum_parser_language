range = pair_sep(int, "..")

# TODO: Investigate error when parens are removed
[XMin, XMax] <- (scan("x=") > range) &
[YMin, YMax] <- (scan("y=") > range) $
{"xmin": XMin, "xmax": XMax, "ymin": YMin, "ymax": YMax}
