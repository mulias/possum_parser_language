input(schedule)

schedule =
  [Weekdays, ...Rows] <- table_sep(word, spaces, nl) &
  [Hours, ...EventRows] <- const(Transpose(Rows)) $
  ZipIntoObject(Weekdays, Map(ZipIntoObject(Hours, _), EventRows))
