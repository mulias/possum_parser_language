unary_intruction = label("type", word < space) + label("var", alpha)

binary_instruction =
  label("type", word < space) +
  label("a", alpha < space) +
  label("b", alpha | int)

array_sep(unary_intruction | binary_instruction, newline)
